services:
    _defaults:
        autoconfigure: true

    Terminal42\Controller\CloseModalController: ~

    Terminal42\DcawizardBundle\EventListener\AddAssetsListener:
        arguments:
            - "@assets.packages"
            - "@request_stack"
            - "@contao.routing.scope_matcher"

    Terminal42\DcawizardBundle\EventListener\AjaxActionsListener:
        arguments:
            - "@request_stack"
            - "@database_connection"

    Terminal42\DcawizardBundle\EventListener\CloseModalListener:
        arguments:
            - "@request_stack"
            - "@cmf_routing.router"
