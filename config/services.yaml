services:
    _defaults:
        autoconfigure: true

    Terminal42\Controller\CloseModalController: ~

    Terminal42\DcawizardBundle\AddAssetsListener:
        arguments:
            - "@assets.packages"
            - "@request_stack"
            - "@contao.routing.scope_matcher"

    Terminal42\DcawizardBundle\AjaxActionsListener:
        arguments:
            - "@database_connection"
            - "@request_stack"

    Terminal42\DcawizardBundle\CloseModalListener:
        arguments:
            - "@request_stack"
            - "@contao.routing.content_url_generator"
